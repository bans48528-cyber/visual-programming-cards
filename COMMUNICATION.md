# Spark_AI 通讯与编译

## 参考与验证边界

协议优先依据相邻项目 `apk-protocol-analysis/APK蓝牙协议分析与桌面版对照.md`，
并交叉检查 `spark-protocol-analysis` 及本机 `E:/Spark AI 1.1.9/Spark-AI/resources`。
没有复制、发布厂商原生二进制，也没有连接实物设备。本机原生编译已成功；通讯测试使用模拟 GATT。

## 连接

- Web Bluetooth 选择器按名称 `Spark_AI` 精确过滤，并通过 optionalServices 授权 FFF0。
- 服务 UUID：`0000fff0-0000-1000-8000-00805f9b34fb`。
- 特征 UUID：`0000fff1-0000-1000-8000-00805f9b34fb`。
- 发现特征、验证写入与 notify/indicate 能力、订阅通知、发送 D0 后才标记通讯就绪。
- 所有指令（包括 B9）优先 writeValueWithoutResponse；仅特征不支持此能力时使用 writeValueWithResponse。写失败不切换方式重发。写入完成不代表设备已执行停止。
- Web Bluetooth 不提供 APK 的 requestMTU(512) 接口；浏览器/系统负责协商。128 字节数据块对应最大 135 字节帧，若系统拒绝写入则中止，不能擅自拆成未经验证的业务帧。
- 已授权设备来自浏览器 getDevices，不等于当前在线或系统安全配对；不自动重连或自动恢复执行。
- 连接建立整体超时 15 秒；写入超时 5 秒时关闭连接，避免挂起写入与后续操作乱序。

## 业务帧

`5A 97 98 LEN CMD DATA... SUM A5`

LEN 是数据字节数，总长度为 LEN+7；SUM 为头至 DATA 的累加和低 8 位。
网页直接传 Uint8Array，蓝牙线上没有 Base64；编译 HTTP 响应使用 Base64 仅用于封装字节码。

| 命令 | CMD | 数据/含义 |
| --- | --- | --- |
| RUN | B6 | 01，运行已有程序；黄色按钮不使用它来冒充新程序上传 |
| STOP | B9 | 01，停止运行，不是可续跑暂停 |
| WATCH | D0 | 01，开始监控 |
| UNWATCH | BA | 01，停止监控，不是停止程序 |
| FILES / VERSION | E7 / EA | 01，查询；常量与封包可用，未自动发送 |
| NAME | DA | UTF-8 文件名，如 `0.o` |
| DATA | AA | 中间字节码块，最多128字节 |
| END / END_RUN | BB / BC | 最后字节码块，BC请求自动运行 |

`SparkProtocol.frame(cmd, data)` 提供基础封包；`uploadFrames(bytes, slot, run)` 提供上传队列。
槽位允许0–10，界面当前使用0。空文件拒绝；整数倍128字节时最后一块直接使用BB/BC，不增加虚构空尾帧。

## 上传与接收

下载运行保留停止状态门槛，并为“等待状态上报”增加专用分支：本连接没有有效状态时先发送一次D0，写入完成后等待500ms；仍无状态则允许进入编译发送，若期间恢复了状态则按恢复后的状态继续判断。最近2秒内收到WillAiState=stop时直接编译；run或过期状态时连续发送5次B9，相邻间隔80ms。第五次写入完成后，若本组B9开始发送后已经收到最新WillAiState=stop则立即编译，否则持续等待新的stop上报，收到后立即继续，最长等待5秒。超时则终止本次发送并提示使用软件或硬件暂停。编译结束、上传开始前再次检查；等待分支仍无状态时允许上传，一旦收到明确状态则只允许最新stop，防止已知运行状态下继续下发。上传前使旧状态失效，避免下一次发送复用旧值。固件状态上报不是独立的物理安全确认，不能保证设备不会在检查之后自行启动。

点击运行后的流程：

1. 检查设备状态；等待上报时发送一次D0并等待500ms，仍无状态则继续；明确运行或状态过期时连续发送5次B9并等待新stop，最多等待5秒。
2. 对当前积木创建快照并生成代码，调用本机原生编译器。
3. 验证编译结果、同一连接、未取消任务和最新停止状态。
4. 上传保持状态监控开启，不发送BA、不等待500ms、不清空接收缓存，保留跨通知的未完成状态消息。
5. DA → 回包 → AA… → 回包 → BC → 回包。
6. 恢复D0监控，显示“已发送并请求运行”，不宣称硬件已成功执行。

每帧回包超时5秒，错误、断连、校验失败均中止，不自动重传。只允许一个上传会话。
停止会取消未发帧、取消等待中的回包，再经写队列发B9；编译中的任务也会失效，不能在停止后晚到并运行。
暂停时若当前界面为“设备已停止”（本连接最近2秒内收到stop），取消本地编译或上传，但不发送D0或B9。若为“等待状态上报”（本连接没有有效状态），先发送一次D0，写入完成后等待200ms，再发送一次B9；其余状态仍经写队列连续发送5次B9，第一条不额外等待，后续每条在上一条系统写入完成后等待80ms再发送。写入失败、连接变化或断连时中止剩余发送，不追加重试。连接时及上传结束后的D0保持不变，不关闭监控。B9仍优先无响应写入；写入完成不等于设备已停止，并存在停止后再次启动的风险。
用户确认B9模拟主机运行物理按键；日志 (11) 中停止状态下发送B9后出现run上报。它不能当作可反复发送的专用停止命令，测试时应确认设备正在运行，避免在停止后再次点击。
2026-09-11 有响应 B9 对照实验已回退：诊断日志 (10) 中一次 B9 写入未完成，阻塞后续点击，5秒后触发客户端超时断连。该实验未消除偶发停止失败，不据此认定固件故障或保证回退后停止成功。
暂停受最新stop状态门槛限制；明确停止时不发送硬件指令，其余分支实际写入时仍检查连接有效性。本地编译/上传始终会取消，发送暂停帧时使旧状态失效，等待设备新的状态上报。
上传保持监控开启；状态JSON不推进上传ACK，但现有兼容ACK仍可能误接受其他有效二进制帧。暂停不可作为急停。
已经交给系统的写操作不能撤回，B9等待该写操作结束；断连时不能保证设备停止，应检查实物。

接收器支持半帧、多帧、带转义/嵌套的连续JSON对象，无须换行。
兼容APK头部第二/三字节各为97或98；额外严格校验A5尾字节及累加和；缓存上限64KiB。恢复早期解析器也意味着接收缓存超限的已知问题仍可能出现。

状态通过 `spark-status` CustomEvent 以原始解析对象发布，不猜测 will_ai 字段的运行状态编码。

**重要：ACK功能码、错误码、包序号仍未确定。** 当前明确采用APK的累加校验兼容策略，
只在上传等待回包期间使用有效二进制帧推进会话。延迟的其他合法二进制包仍可能被误当作ACK；
停止监控不能从根本上消除此风险。`allowUnverifiedAck:true` 必须显式开启，未开启则拒绝上传。
需要实机抓包后补充 ACK 命令/错误码匹配；校验通过并不证明固件写入成功。

## 编译与积木映射

`program-codegen.cjs` 只生成验证过形态的固定调用，不接受任意Python源文件或文件路径。
证据来自桌面 `lib.min.js` 中的 `motor_box`、`motor_specifiedunit`、`motor_stop`、`control_wait`、
`handleResult`；这些代码使用固件预置的 `_motor` / `_os`，没有新增猜测的import。
其余接口交叉核对本地 `spark-ai-python-blocks/docs/ai_rules/supported_functions.md`、硬件概览和示例，
以及桌面生成器的 `combined_mov_dir_power_seconds`、`sensing_mainIsPress`、`cmp_lux`、`play_muic` 和 `stringToHex`。

| 积木 | 当前生成 |
| --- | --- |
| 电机功率 | 全局 `motor_power` 赋值，25/50/75/100%，不启动电机 |
| 电机正转 / 反转 | `_motor.run_for_power_seconds(port, motor_power/-motor_power, duration)` |
| 电机停止 | `_motor.stop(port)` |
| 单电机持续正转 / 反转 | `_motor.run_power(port, motor_power/-motor_power)`，无时间参数 |
| 组合持续移动 | `_motor.mov_dir_power(direction, combo_power)`，方向为前进/后退/左转/右转 |
| 等待 | `_os.sleep_s(duration)` |
| 次数循环 / 无限循环 | for range / while True，循环内加入1ms等待 |
| 组合功率 | 独立 `combo_power` 赋值，25/50/75/100% |
| 组合运动 / 转向 | `_motor.mov_dir_power_seconds("advance/retreat/left/right", combo_power, duration)`，四个方向分别生成 |
| 组合停止 | `_motor.mov_stop()` |
| 超声条件等待 | `while not (_ultrasion.cmp_value(port, operator, distance))`，循环内等待1ms |
| 灰度条件等待 | `while not (_color.cmp_lux(port, operator, value))`，无cm单位 |
| 外接触碰等待 | `_touch.state(port)`，松开条件取反 |
| 主机按键等待 | `_key.key_mast("left/right", 1)`，左右键分别生成 |
| 演奏音 | 1–7对应c/d/e/f/g/a/b，`_beep.play_muic(note, beats)`；可选0.25/0.5/1/2拍，默认0.25拍 |
| 点阵 | `_matrix.show`，5×5图案上下补空行，输出7个5位行值，左像素为bit0 |

电机端口E/F/G/H对应4/5/6/7；传感器端口A/B/C/D对应0/1/2/3。旧作品电机A–D按顺序迁移为E–H。
50%为本项目明确选择的默认功率，不是测得的硬件默认值。
蓝色功率积木不区分端口，对后续单电机类E–H动作生效，直到下一个蓝色功率积木覆盖；循环内按运行顺序赋值，每次重新运行默认重置为50%。粉色组合电机功率块使用独立的combo_power变量，不影响单电机功率。
按用户明确接线，组合电机固定左E右F；出现组合动作时仅初始化一次 `_motor.pair(4, 5, 1)` 和 `_motor.mov_set_stop_module(1)`。
模式1沿用参考示例，物理转向和停止模式效果仍需实机验证；功率块自身不启动电机。
物理转向仍依赖电机接线和固件，需实机确认。
传感器采用“等待条件成立后继续”的顺序语义，严格相等不添加隐含容差，不会自动停止之前的电机动作。
传感器缺失或条件一直不成立可能持续等待；使用B9停止。音符拍数不等同于秒。
主机点阵走Python `_matrix.show`，不是APK针对其他矩阵的E0九字节命令；旧25像素存档不改写为35像素。
持续动作不生成等待或自动停止，启动后继续执行下一块，可接传感器条件等待后接对应停止块。没有停止指令时不能假定程序末尾或断连会停止电机；实机测试应准备硬件断电措施。
现有22种积木均支持生成；未知积木、非法端口/参数/图案仍整单拒绝，错误包含积木位置。

## 本机运行

保持页面服务在 `http://127.0.0.1:4173/`，另运行：

```powershell
node compiler-server.cjs
```

编译服务只监听 `127.0.0.1:4180`，允许4173的localhost/127.0.0.1来源，拒绝其他Origin、Host及非JSON请求。
仅支持电脑本机发送；手机编译服务、HTTPS部署或WASM尚未接入。静态网站部署不会自动获得原生编译能力。

默认加载安装目录中的 `pika_bytecode_gen.node`；其他电脑通过环境变量 `PIKA_ADDON_PATH` 指定。
直接加载已验证可用的N-API二进制路径，避开厂商index.js对app.asar路径重复替换的问题。
每次编译在独立子进程和临时目录完成，15秒超时，源码和产物随后删除。
请求最大256KiB、最多1000积木、嵌套20层、字节码最大1MiB；这些是本项目保护上限，不是固件容量规范。

## 回归验证

```powershell
node tests/protocol.spec.cjs
node tests/bluetooth.spec.cjs
node tests/hardware-flow.spec.cjs
node tests/all-cards.spec.cjs
```

除protocol外的测试需Playwright及Edge；hardware-flow需启动本机编译服务。
all-cards逐一编译22种积木，并验证组合配对、持续动作与停止顺序、端口、条件、音符拍数、不对称点阵、混合嵌套和错误路径。
覆盖固定帧、128边界、半包/多包/JSON、校验错误、超时、取消、断连、发现失败、真实编译和模拟上传。
未验证无线MTU、固件字节码兼容性、真实ACK、物理运行及停止结果。
